package com.godaplayer.app.ui.theme

import androidx.compose.ui.graphics.Color

object GodaColors {
    // Background colors
    val PrimaryBackground = Color(0xFF0D0D0D)
    val SecondaryBackground = Color(0xFF1A1A1A)
    val TertiaryBackground = Color(0xFF262626)

    // Text colors
    val PrimaryText = Color(0xFFE0E0E0)
    val SecondaryText = Color(0xFF888888)
    val DisabledText = Color(0xFF4A4A4A)

    // Accent colors
    val PrimaryAccent = Color(0xFF00FF88)
    val SecondaryAccent = Color(0xFF00AA55)
    val Warning = Color(0xFFFFAA00)
    val Error = Color(0xFFFF4444)

    // Additional colors for UI elements
    val Transparent = Color.Transparent
    val DividerColor = Color(0xFF333333)
    val BorderColor = Color(0xFF333333)
}
